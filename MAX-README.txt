METWIPE MAX — BUILT ON THE USER-APPROVED ULTIMATE BASELINE

IMPLEMENTED + ANIMATED
- JPEG detailed EXIF/GPS/device/date scan, lossless metadata stripping, verification
- PNG metadata chunk cleaner + verification
- WebP EXIF/XMP cleaner + verification
- MP3 ID3v2 + ID3v1 removal + verification
- WAV RIFF metadata chunk cleaning + verification
- DOCX core document-property cleaning using browser-native OOXML ZIP processing
- XLSX core document-property cleaning
- PPTX core document-property cleaning
- Multi-file batch cleaning + ZIP + privacy receipt
- Visible staged Scan → Detect → Clean → Verify animations
- SEO pages for each implemented format
- Ecrin Labs branding, email, robots, sitemap template, ads.txt template, security headers

INTENTIONALLY NOT CLAIMED AS WORKING
- PDF: safe production-grade PDF metadata removal needs a PDF parser/rebuilder.
- MP4/MOV: safe metadata removal requires an ISO-BMFF remux/rebuild engine.
These are not faked in this package.

BROWSER NOTE
OOXML DOCX/XLSX/PPTX cleaning requires modern browser support for CompressionStream/DecompressionStream (current Chromium-class browsers are the intended target).

LAUNCH
Replace metwipe.com after selecting the final domain. Add real AdSense code/publisher ID only after approval and update the production privacy/cookie/consent text.
